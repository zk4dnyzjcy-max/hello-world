# A Postgres connection string. On Vercel: Project -> Storage -> add a Postgres
# database (Neon), then copy the connection string it gives you.
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"
